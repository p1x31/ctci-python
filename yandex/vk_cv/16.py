a = '10'

print(a+a)

print(a*2)

#find char 1 in string a


print(a.find('10'))
#print(a*a)

#what git fetch does is it fetches the changes from the remote repository to the local repository


# what git branch --set-upstrea-to origin/master does is it sets the upstream to the master branch of the origin repository

# what git branch --track origin master does is it tracks the master branch of the origin repository